Defensive correctness and completeness audit — receipt 0.7 M3, round 2

Verdict: **approve**. The folded record resolves round 1's transport finding by explicitly choosing context-scoped availability, documenting the lost cold-process refusal and possible verdict differences, and requiring separate approval and implementation evidence. The choice is sound for authenticating selected objects. It does not establish fresh-process availability or a filesystem snapshot of the object store. This review approves the design record; it does not supply the maintainer's D1/A7 approvals or certify unimplemented context code.

The reviewed artifact is `docs/design/0.7-m3-repository-context.md` at `9401e7bcad67e082bed967acb081392eb8bb2c48`. Locators below refer to that revision. Unqualified production paths are under `src/receipt/`, test paths under `tests/`; M1 and M3 designate their respective design records. Evidence is committed under `audit/m3-design-r2/`. The record, production modules, and tests were not edited. One reviewer performed this offline audit without subagents or background jobs.

**Scope — CORRECTED against the request's older counts.** This revision has 731 lines, M3-D1–D18, seven migration rows, and twelve risks. The twelve screen disagreements, conversion table, and precedence D10 belong to M1; M3-D10 concerns mutable reference drift. Both lists are assessed below. Own `git diff` commands confirm `src` and `tests` equal both `9dc1f85` and `f797f72`. Collection reports 3,297 cases: 3,189 non-harness plus 108 harness. The requested 1,466-test count is historical. M3 states the current gate correctly at 451–489.

**Transport decision — CONFIRMED; proposed implementation remains UNVERIFIED.**

The record's supplied packed-store driver was read and rerun unchanged, using this checkout's imports. Its output was:

```json
{"different_pids": true, "independent_entered_cold_transport": {"exception": "receipt.snapshot.SnapshotError", "message": "object c2981a9931b383b5eb128dc5e3505654ab5269b6 is unavailable"}, "removed_pack_files": 3, "warm_transport_cold_python_headers": {"value": "payload\n"}}
```

An independently written `transport_matrix.py` executed 96 fresh raw-index cases: three storage layouts, four mutations, four warmth pairs, and both read orders. Replacement packs were constructed outside the object store with `pack-objects`, then installed after unlinking the original packs. Both entered readers had distinct PIDs; both Python header maps were cleared after mutation. All 96 outcomes matched these rules:

| Storage and mutation | Independent result, both observation orders |
| --- | --- |
| Unchanged store or replacement retaining the target | Both readers return `payload\n`, regardless of warmth. |
| Loose-only removal or replacement omitting target | Both refuse the exact unavailable-object message, regardless of warmth. |
| Packed-only removal or replacement omitting target | A warmed reader returns `payload\n`; a cold reader refuses. Both asymmetric warmth permutations reproduce. |
| Packed-plus-loose, with the loose target retained | Both readers return `payload\n` through pack removal or either replacement. |

These are current-reader transport experiments, not runs of `RepositoryContext`. Logs are `packed-store-probe.txt` and `transport-matrix.jsonl` in the evidence directory. The independent matrix extends the record's 48-case evidence to reverse observation order; it does not establish the remaining selection-warming or new-account traces.

The returned payload authenticates: `_BatchReader.info` reaches a fresh header request after clearing `_headers` (`snapshot.py:1215–1229`); `consume` checks role and limit, frame type/size, trailer, and the hash of the Git object header plus payload (`1231–1291`). Missing headers refuse at `1195–1197`. The focused tests also passed actual tampered-tree/blob and loose/packed store checks (`test_snapshot_security.py:714–853`). Preserving a readable authentic object through an unlinked pack does not make altered bytes acceptable.

The lost refusal **matters observably**: today's independent cold B can fail with `object … is unavailable`, while a shared warm child can supply B's requested authentic object and let verification continue. A later refusal can therefore win, work can increase, or an otherwise passing verification can finish PASS. The raw probe establishes that necessary transport difference; it does not itself execute a complete composed PASS/FAIL comparison. M3:292,301–311,343–360,493–495,534–537 explicitly requires those implementation traces and forbids normalizing the refusal into success.

An auditor may conclude that required reads authenticated the selected OIDs under the context's lifetime and that the reached policy, admission, and finalization checks succeeded. The auditor may not conclude that a newly started Git process could retrieve every object at publication time, that all objects remained in the directory throughout verification, or that the store was a stable filesystem snapshot. Today's warmed reader already exhibits retained availability, and its finalizer checks sentinels/configuration rather than revalidating every object's continued presence (`snapshot.py:2014–2081`). Optional store verification remains a separate bounded check at its reached barrier (`2642–2754`); A7 cannot replace it with cache success.

For that stated claim, context-scoped availability is fail-closed with respect to authenticity, role, structural admission, and policy completion. It deliberately relaxes independent-process availability behavior. I favor the shared child under the explicit A7 gate. A child per selected view is necessary if the maintainer requires the old independent availability histories; it is not necessary to authenticate the requested bytes. M3:294–313 states that choice and its consequences precisely enough for an explicit maintainer decision. No approval of that behavior change is inferred from passing unchanged-store tests.

**1. Screen census — CONFIRMED, with one pin-strength qualification.**

The following twenty independently inspected sites cover all six requested modules. E means complete exception/value equality through a literal assertion or committed golden, after only explicit fixture-identity substitutions. S means a substring/unanchored regex, which cannot establish exact bytes. The E matrices below ran in this review. `m1_fixture.py:232–264` establishes the golden comparison semantics; the actual strings remain in the unchanged `m1_expected/*.json` files.

| Site and status | Condition and covered tree region | Exact refusal expression; test pin |
| --- | --- | --- |
| 1 CONFIRMED — `verify.py:671–701` | Custody names over five protected prefixes and their actual immediate ancestor listings, retaining tree/empty-tree records; after optional history. | Alias renderer `release_chain.py:2301–2304`: `f"index carries an alias of a protected path: {finding.path} " f"(for {finding.target} at {finding.prefix})"`. E: `test_m1_disagreements.py:30–47`, `d01.json`/`d02.json`, including custody phase. |
| 2 CONFIRMED — `verify.py:703–726,89–94` | Exact journal/prefix lookup; missing entry masking followed by regular state-leaf admission. | `f"state file is missing or not a regular file: {display}"`, `display = relative.as_posix()`; shape texts `f"state file is a symlink: {finding.path}"` and `f"state file is not a regular file: {finding.path}"`. E: `test_m1_compat_golden.py:296–309`, both leaves/all shapes/CLI. |
| 3 CONFIRMED — `release_chain.py:2207–2222,2257–2260` | Every candidate non-tree release descendant must be regular before any base comparison. | `f"release path is a symlink: {finding.path}"`; `f"release path is not regular: {finding.path}"`. E: history goldens at `test_m1_compat_golden.py:312–327`. |
| 4 CONFIRMED — `release_chain.py:2225–2249` | Sorted base release leaves: base regularity, deletion, mode equality, then OID equality. | `f"base release entry has non-regular git mode {prior.mode}: {relative}"`; `f"existing release file was deleted relative to {base.commit}: {relative}"`; `f"existing release file mode changed relative to {base.commit}: {relative} ({prior.mode} -> {current.mode})"`; `f"existing release file bytes changed relative to {base.commit}: {relative}"`. E: same history matrix. |
| 5 CONFIRMED — `release_chain.py:2269–2311` | Mapping facade delegates configured aliases, scoped names/siblings and release/manifest DOS suffix obligations. | Row 1 alias expression; `"release root contains an entry whose short-name alias " f"would carry a pinned suffix: {finding.path}"`; otherwise `ReleaseChainError(finding.detail)`. E: `test_m1_disagreements.py:40–47,95–102`; precedence at `test_m1_precedence.py:22–47`. |
| 6 CONFIRMED — `release_chain.py:540–570` | Direct-directory manifest descent: absence, wrong ancestor, wrong leaf, other `lstat` failure. | `"release manifest path ancestor is not a directory: " f"{'/'.join(walked)}"`; `f"release manifest path is not a regular directory: {current}"`; `"cannot stat release manifest path: " f"{'/'.join(walked)} ({exc.strerror})"`. E: `test_m1_compat_directory.py:18–38`, including permission/ENOTDIR injection. |
| 7 CONFIRMED — `append_gate.py:796–816,787–790` | Two exact state leaves; before broad screens and gate-only return. | Row 2's missing expression; `f"state file is a symlink: {finding.path}"`; `f"state file is not a regular file: {finding.path}"`, as `AppendError`. E: `test_m1_compat_golden.py:296–309`. |
| 8 CONFIRMED — `append_gate.py:871–890,783–786` | Protected ancestors before aliases/names, using the complete tree listing including empty trees. | `SnapshotError(f"state path has a symlinked component: {finding.path}")`; `SnapshotError(f"tree path ancestor is not a directory: {finding.path}")`; name exceptions become `AppendError(str(exc))`. E: `test_m1_disagreements.py:84–93`; same-stage swaps at `test_m1_precedence.py:22–47`. |
| 9 CONFIRMED — `append_gate.py:912–924,791–793` | Push-time release leaves, excluding the exact manifest path so its directory diagnostic stays separate. | `f"release path is a symlink: {finding.path}"`; `f"release path is not regular: {finding.path}"`. E: `test_m1_precedence.py:153–159`; manifest distinctions at `128–138,177–186`. |
| 10 CONFIRMED — `append_gate.py:893–909,1120–1130` | Regular entries selected by append's protected-surface/export predicate become attribute targets before surface classification/early return. | `protected_tree.py:1313–1322`: `f"transforming attribute {finding.name} applies to protected path {path}"`, `path = finding.raw_path.decode("utf-8", errors="strict")`; decoding failure: `"tree entry name is not valid UTF-8 for quoting"`. E: `test_m1_disagreements.py:104–118`; quoting at `test_m1_compat_golden.py:225–229`. |
| 11 CONFIRMED — `corpus.py:1424–1437,1372–1375` | Complete binding-tree name admission before sibling collisions; scope exceeds custody. | `"directory holds two entries a case-insensitive filesystem " f"would merge: {_quoted(_under(finding.parent, finding.other_name))} and " f"{_quoted(_under(finding.parent, finding.name))}"`. E: `test_m1_disagreements.py:30–37`, `d01.json`. |
| 12 CONFIRMED — `corpus.py:1450–1473,1380–1387` | Pinned content-root component spelling, existence and directory shape. | `f"tree entry {_quoted(finding.name)} aliases the pinned content " f"root component {_quoted(finding.target)} on a case- or " "normalization-insensitive filesystem"`; `f"pinned content root is absent from the tree: {finding.path}"`; `f"pinned content root is not a directory: {finding.path}"`. E: `test_m1_disagreements.py:40–47`; `test_m1_compat_golden.py:232–239`. |
| 13 CONFIRMED — `corpus.py:1464–1473,1388–1397` | Content descendants: gitlinks, selected suffix/DOS obligations and regular-file modes; suffixless symlink exception retained. | `f"content root contains a gitlink: {_quoted(finding.path)}"`; `"content root contains a file whose short-name alias " f"would carry a pinned suffix: {_quoted(finding.path)}"`; `"content root contains a symlink where a regular file was " f"recorded: {_quoted(finding.path)}"`; `f"content root contains a non-regular file: {_quoted(finding.path)}"`. E: `test_m1_disagreements.py:71–81,95–102`; directory branch at `test_corpus.py:452–474`. |
| 14 CONFIRMED — `corpus.py:1512–1538,1398–1399` | Sorted exact attested paths; lookup `SnapshotError` is masked before leaf-mode admission. Empty input performs no read. | `f"bound file is missing or not a regular file: {path}"`; `f"bound file is not a regular file: {finding.path}"`. E: `test_m1_compat_golden.py:280–293`, plus executed empty/masked lifecycle controls. |
| 15 CONFIRMED — `snapshot.py:174–198` | Reached exact ancestor in lookup/listing versus protected source/export descent; no speculative whole-tree traversal. | `f"tree entry does not exist: {finding.target}"`; `f"protected path ancestor is not a directory: {finding.prefix}"`; `f"state path has a symlinked component: {finding.prefix}"`; `f"tree path ancestor is not a directory: {finding.prefix}"`. E: `test_m1_compat_golden.py:117–136`, six shapes/four operations. |
| 16 CONFIRMED source/probe; CORRECTED pin strength — `snapshot.py:2204–2236` | Payload entry concrete type, selection token and unchanged metadata, even for equal-OID entries. | `"blob entry must be a GitEntry"`; `"GitEntry does not belong to this snapshot"`. The latter is S at `test_snapshot_features.py:966–970,994`; it is not a full-byte pin. The former has no located text assertion outside the frozen legacy body (UNVERIFIED pin). This review's D3/identity probes captured the complete latter text. |
| 17 CONFIRMED — `snapshot.py:2774–2814`; `protected_tree.py:1095–1144` | Attribute collection admission and selected-tree source presence/mode/type/byte/rule admission, including negative source cache entries. | `"attribute paths must be an iterable of paths"`; `f"unsupported .gitattributes entry at {path}: mode {raw.display_mode}"`; transform text in row 10. E: `test_m1_compat_golden.py:84–114,216–222`; D8 source matrices. |
| 18 CONFIRMED — `_names.py:220–232` | One raw component under either repertoire: type, empty, dot/dotdot, NUL, slash. | `f"{label} must be bytes: {value!r}"`; `f"{label} is empty"`; `f"{label} is a dot component: {value!r}"`; `f"{label} contains NUL: {value!r}"`; `f"{label} contains '/': {value!r}"`. E: `test_m1_compat_golden.py:24–71`, `bytes_*` cases. |
| 19 CONFIRMED — `_names.py:236–268,298–320,175–180` | Component repertoire versus export portability; strict UTF-8 for folding; ASCII case only. | `f"{label} is not valid UTF-8 under name repertoire {selected!r}: {raw!r}"`; `"tree entry name is not valid UTF-8 for folding"`; portable text `f"{label} is not a portable name (ASCII letters, digits, '.', '_' and " "'-', not ending in '.', not a Win32 device name)" f"{context}: {value!r}"`, where `context = "" if repertoire is None else f" under name repertoire {repertoire!r}"`. E: primitive goldens and M1-D3/D4. |
| 20 CONFIRMED — `_names.py:348–403` | Immediate sibling sequence: per-name admission/fold/collision interleaving; exact duplicate versus distinct folded collision. | `f"{label} contains a duplicate entry name: {text!r}"`; `f"{label} contains names that merge under ASCII case folding: {previous_text!r} and {text!r}"`. E: primitive `siblings_duplicate`/`siblings_fold`, M1-D1 and precedence controls. |

The corpus `_quoted` expression is bounded: `repr(value)` through `MAX_QUOTED_TEXT`, otherwise `f"{text[:MAX_QUOTED_TEXT]}…[{len(text) - MAX_QUOTED_TEXT} more characters]"` (`corpus.py:559–562`). These renderers' quoting, spaces and truncation are part of the compatibility contract. Separately, environment admission is ordered `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`; mere presence refuses with `f"{name} is set in the environment and would redirect git reads; unset it"` (`release_chain.py:2171–2194`). It covers process input, not a tree region.

**2. Disagreements — CONFIRMED for reproduced legacy observations; future context gates remain UNVERIFIED.**

The appendix produced `D1 ['select', 'select', 'enter', 'close']`; its second Python block was extracted directly from Markdown and executed. Both blocks compile. The additional driver supplies D2/D5/D8/D10/D13 rather than attributing them to the appendix. Results are in `appendix.txt`, `probes.txt`, and `identity-probe.txt`.

| M3 item | Independent result and preservation judgment |
| --- | --- |
| D1 CONFIRMED | No refusal; four ambient captures observe select/select/enter/close. `_git_environment` reads `os.environ` at call time (`snapshot.py:486–505`). Capture-once is a deliberate correctness correction, not unchanged behavior. |
| D2 CONFIRMED | A selected before harmless `m3.probe=changed` and B selected afterward have different config tuples. B closes; A refuses `repository configuration changed during verification`. M3:226–229 retains the finalization barrier for harmless differences; no foreign baseline may be silently adopted. Restoration/race expansion remains a gate. |
| D3 CONFIRMED | Equal snapshots and entries, different policy subjects/PIDs; foreign blob entry refuses `GitEntry does not belong to this snapshot`. Initial tree bytes were 375, consistent with the record's fixture-dependent qualification. |
| D4 CONFIRMED exercised identities; UNVERIFIED linked-worktree expansion | Distinct commits sharing one tree and a second repository with that tree yield three subjects. Both foreign-entry reads refuse the D3 text. Private/common worktree permutations remain implementation evidence. |
| D5 CONFIRMED | All six 6+5/limit-10 probes refuse before merging, retain 6/5 and separate roots. Texts: `tree paths exceed the snapshot budget of 10 bytes`; `attribute bytes exceed the snapshot budget of 10 bytes`; `attribute rules exceed the snapshot budget of 10 rules`; `attribute matching exceeds the work budget of 10 steps`; `content bytes exceed the snapshot budget of 10 bytes`; `materialized bytes exceed the budget of 10 bytes`. Order is fixed at `snapshot.py:2119–2154`. |
| D6 CONFIRMED | At path ceiling 19, direct unlinked history passes with candidate/base counts 10/10. Explicit linkage refuses `tree paths exceed the snapshot budget of 19 bytes` at 0/10. M3:231–240 correctly activates totals only at the old barrier. |
| D7 CONFIRMED | Supplied driver and independent 96-case matrix reproduce the folded transport contract. A7 is an explicit exception to availability equivalence, not an authentication/charge waiver. |
| D8 CONFIRMED | Linked source-absent/source-transforming pair gives PASS then `transforming attribute filter applies to protected path protected.txt`; reversing subjects reverses those outcomes. Source charges are 0/27 versus 27/0. Selected source presence and absence cannot be shared by raw path alone. |
| D9 CONFIRMED | Each linked account admits 22 attribute bytes, one rule and 28 matching steps; shared store reports two pure rule evaluations. Link-after completed-fact tests pass. Sharing computation cannot refund admission. |
| D10 CONFIRMED | Replacing private `a._state.work` lets policy listing succeed while `policy.work is a.work` is false and the replacement account is outside the pool. Immutable registration addresses an internal invariant hazard; repository bytes do not perform this mutation. |
| D11 CONFIRMED | Closed reader/policy: `snapshot must be entered before object reads`; reentry: `snapshot is closed`. Abandoned policy: `snapshot stream was abandoned`; linked independent sibling still reads. Same-context terminality must not erase legacy isolation. |
| D12 CONFIRMED | Unbounded repeated calls charge matching 28 then 56; paths 27 then 40; source bytes/rules remain 22/1. Ceiling 40 makes the second call refuse `attribute matching exceeds the work budget of 40 steps` at exactly 40. All 57 stopping ceilings and checkpoint controls passed. |
| D13 CONFIRMED | After one unexhausted digest item on A, B reads successfully; A's unrelated read refuses `snapshot stream was abandoned`; B still succeeds. Different PIDs substantiate the legacy failure-domain boundary. |
| D14 CONFIRMED inspected contract/exact-command control; UNVERIFIED full proposed matrix | The two-snapshot store command/environment test passed (`test_snapshot_security.py:919–1054`). Source readiness/attempt/head checks are per snapshot (`snapshot.py:2642–2754`); new-context per-account attempt ownership remains to be implemented and tested. |
| D15 CONFIRMED selected controls | Five subclass lifecycle/no-authority controls and composition/import compatibility passed. M3 preserves protocol dispatch before concrete context access; the complete new classmethod-substitution matrix remains future evidence. |
| D16 CONFIRMED | All four `test_verify_binding_seam.py` cases passed, including pre-import and simultaneous public-reference replacement. Keep the original sentinel and operative callable dispatch. |
| D17 CONFIRMED source/existing close control; UNVERIFIED new-finalizer matrix | Closing-config test passed. `verify.py:835–849` retains history, clears chain/corpus, removes other passes, fails custody and leaves binding incomplete. The record correctly preserves actual rendering and still requires every post-pass finalizer fault combination. |
| D18 CONFIRMED exercised legacy scope; UNVERIFIED new-context completeness | Empty-tree, ancestor, export, pre-genesis, foreign/finished shape and incomplete-authority controls passed. `require` rejects incomplete obligations (`protected_tree.py:1836–1855`). Context reuse cannot imply another evaluator completed its plan. |

The requested older M1-D1–D12 also all reproduced through `test_m1_disagreements.py` and `test_m1_precedence.py`: 55 disagreement and 36 precedence cases. Their fixtures use raw cacheinfo/tree construction and assert topology before policy (`test_m1_disagreements.py:17–26`). Representative exact outcomes follow; full class/phase/value equality was checked against the committed goldens.

| M1 item / status | Refusing pass, text, and preserved decision |
| --- | --- |
| D1 CONFIRMED | `unused/Pair.txt` and `unused/pair.txt` pass custody/chain/base/export/append preflight; binding refuses `directory holds two entries a case-insensitive filesystem would merge: 'unused/Pair.txt' and 'unused/pair.txt'`. Empty release sibling collisions instead refuse custody. Preserve scope and renderer. |
| D2 CONFIRMED | `Releases/other.txt` refuses custody/chain/base/append with `index carries an alias of a protected path: Releases/other.txt (for releases at releases)`; binding uses `directory holds two entries a case-insensitive filesystem would merge: 'Releases' and 'releases'`; standalone export passes. |
| D3 CONFIRMED | Posix-bytes `releases/bad?.txt` passes name-only/binding obligations but physical export and composed custody/base refuse row 19's portable expression, with `value='bad?.txt'` and `repertoire='posix-bytes'`. Gate-only append remains accepted. |
| D4 CONFIRMED | Raw `unused/bad\xff` refuses binding/append preflight with `tree entry name is not valid UTF-8 for folding`; custody/base/export pass. Distinct NFC/NFD UTF-8 names remain accepted. |
| D5 CONFIRMED | `rules/module` gitlink refuses binding with `content root contains a gitlink: 'rules/module'`; selected content symlink uses `content root contains a symlink where a regular file was recorded: 'rules/tax/rate.yaml'`. Other scopes retain their own mode obligations. |
| D6 CONFIRMED | Blob ancestor `receipt` refuses composed custody/append with `tree path ancestor is not a directory: receipt`; base/export use `protected path ancestor is not a directory: receipt`. Supplied-journal binding retains its different input contract. |
| D7 CONFIRMED | Portable `rules/hidden.ymlx` with `.yml` pinned refuses binding with `content root contains a file whose short-name alias would carry a pinned suffix: 'rules/hidden.ymlx'`; posix-bytes retains its PASS. |
| D8 CONFIRMED | A transform on `rules/tax/rate.yaml` refuses append preflight only: `transforming attribute filter applies to protected path rules/tax/rate.yaml`. A release-anchor transform also refuses custody/base at that anchor's exact path. Raw binding and standalone materialization retain their narrower obligations. |
| D9 CONFIRMED | Tree-owned disjoint anchor symlink refuses `base tree entry has non-regular mode 120000: separate-anchors/unused-symlink`; caller-supplied anchors pass. Anchor origin remains part of the plan identity. |
| D10 CONFIRMED | Bad portable release name plus `releases/a-link`: name-consuming passes refuse portable spelling first, while standalone materialization refuses `base tree entry has non-regular mode 120000: releases/a-link`. Cross-stage and same-stage swap tests also preserve exact precedence and zero premature attribute reads. |
| D11 CONFIRMED | Direct-directory ordinary folded extras/attribute text pass; direct state symlink refuses `required state file is missing or non-regular: <ROOT>/receipt/corpus-journal.jsonl`, with the actual absolute root substituted. Git-composed scope stays distinct. |
| D12 CONFIRMED | The repeated 28→56 / ceiling-40 result matches the M3 reproduction above. Cache reuse preserves the failed step and counters. |

M1:303–314 explicitly preserves these pass/text distinctions. M3:388–407,493–499 keeps those obligations and their compatibility gates; it does not introduce a replacement twelve-row “M3 decision” table that merges their outcomes.

**3. Consumer census — CONFIRMED at the inspected offline refs; consumer-suite execution UNVERIFIED.**

Only `git show` and `git grep` were used in consumer repositories; no checkout or network operation occurred. Chronicle's requested ref resolves to `e9b803b609e0282f8ea33cd2c07e47e869b0739c`. Its runtime shims catch exception categories and render messages; complete-byte matching resides in tests.

- `scripts/verify_release_chain.py:21–22,44–67` imports `receipt.release_chain`, `SnapshotError`, `TreeSnapshot`; reads/reexports `CRYPTOGRAPHY_AVAILABLE`, `DEFAULT_CLOCK_SKEW_SECONDS`, `MAX_FUTURE_SECONDS`, `MAX_RELEASE_INDEX`, `MANIFEST_RE`, `PRODUCER_SIGNATURE_BYTES`, `PRODUCER_SIGNATURE_RE`, `SHA256_RE`, `STRICT_UTC_RE`, `TIME_STAMP_RE`, `AnchorSpec`, `ChainSpec`, `ChainVerification`, `GitEntry`, `ReleaseChainError`, `ReleaseRecord`, `jsonl_line_offsets`, `manifest_filename`, `parse_created_at`, `producer_signature_path_for_manifest`, and `sha256_bytes`. Private `_receipt_re` at 51 and `_format_time` at 278 are real consumer obligations.
- Its wrappers at 70–207 call `validate_manifest_schema`, `load_manifest`, `receipt_paths_for_manifest`, `verify_producer_signature_bytes`, `verify_producer_signature`, `verify_receipt`, `verify_release_receipts`, `verify_release_chain`, `verify_release_history_immutable`, and `verify_base_release_chain`. At 245–260 it independently enters candidate/base and calls ancestry before borrowed verification.
- `scripts/check_thesis_facts_append.py:50–51,70–73,408–437` imports `receipt.append_gate`, `MANIFEST_RE`, `ReleaseChainError`; reexports `AppendError`, `AppendGateSpec`, `AppendGateVerdict`, `reject_non_append_bytes`; calls `expected_assertion_version_id`, `effective_current_rows`, `check_rows`, `verify_append_gate_verdict`. At 508–514 it consumes summary and candidate/base commit/tree fields.
- `scripts/receipt_pins.py:7–8` imports `AppendGateSpec`, `AnchorSpec`, `ChainSpec`. `scripts/canonical_json.py:7–13` imports `canonical_bytes`, `canonical_sha256`, `canonical_stringify`, `main`, `utf16_sort_key`.
- Runtime failure wrappers are `f"release chain verification failed: {exc}"` (`verify_release_chain.py:270–272`), `f"{VERDICT_REFUSAL_PREFIX}{exc}"` with `VERDICT_REFUSAL_PREFIX = "thesis-facts append check failed: "`, and the shim's `"thesis-facts append check refused: "` prefix (`check_thesis_facts_append.py:184–185,502–507`). `print` adds the newline. Exception category can change these bytes even with unchanged `str(exc)`.
- `tests/test_receipt_shim_transparency.py:146–161` compares complete exit/stdout/stderr after only OpenSSL error-queue ID normalization. Lines 164–216 separately assert the successful candidate/base tail against actual IDs and compare the remaining stdout. Isolation assertions include `symlink`/`not regular` plus paths at 364–412; equal stderr and `change truncates the ledger` at 823–824; `change rewrites existing line {PREFIX_LINE_COUNT + 1}` and `change truncates the ledger: {CANDIDATE_LINE_COUNT} -> {CANDIDATE_LINE_COUNT - 1} rows` at 873–918.

Axiom-encode `origin/main` is `1daadad265dc9eaa1ab737df87b453a5e5e0cb46`; only `receipt.sign` is imported at `src/axiom_encode/cli.py:46` and `tests/test_receipt_sign_adoption.py:7`. Its local `main` is `427edd81aec1c334d6e97db30d7b0a461744b64c` and the corresponding Python import grep returns no receipt imports. Thesis at `/Users/maxghenis/ThesisInstitute/thesis` has no local `main`; `origin/main` is `aad708550b73d0b624085fbbd04f5f8a1f85ef04`. Only `receipt.sign` symbols are imported at `scripts/sign_record_snapshot.py:265–270`, `scripts/verify_record_chain.py:286`, and `tests/test_producer_signing.py:23–27`. This confirms signing-only adoption on available remote-tracking main refs, not live remote state. Evidence: `consumers.txt`.

**4. Design sufficiency — CONFIRMED for the proposed contract, with implementation gates retained.**

`ProtectionPlan` expresses ordered obligations, scopes, mode roles, attribute targets and the complete fingerprint (`protected_tree.py:44–126`). Findings retain nested positions (`130–155`); `finding_for` orders by stage then position, while `require` separately denies incomplete authority (`1825–1855`). Lazy evaluation is compatible with all executed M1-D10 precedence pins only if later computation and I/O are deferred too. Computing everything eagerly and delaying the exception would change charges, read failures, or abandonment. The actual cross-stage tests assert zero attribute-source calls before the earlier barrier (`test_m1_precedence.py:54–82`).

No unintended second generic name/mode/alias/attribute algorithm was found among the sampled converted sites. Some decision loops intentionally remain: immutable-history comparisons (`release_chain.py:2225–2249`), tombstone membership over the shared fold index (`corpus.py:1490–1501`), append surface-to-target selection (`append_gate.py:893–903`), and physical descent/closed manifest grammar (`release_chain.py:544–609`). M1's conversion table explicitly retains those boundaries at 535–554, along with configuration-constructor and evidence-serialization admission. `_names._screen_sibling_names` is the central primitive loop, not a second verifier implementation. `DirectoryEvidence` (`protected_tree.py:1887–1900`) cannot authorize a Git policy pass or replace later descriptor/identity/read-once checks. Literal elimination of every local decision would contradict those retained contracts.

D12 is implementable without changing its existing behavior: it is already implemented by completed rule checkpoints, arithmetic admission when affordable, and reexecution of the exhausting rule (`protected_tree.py:1241–1279`). The old/new fixture compares both accounts, logical read events, failure events and all work fields (`test_protected_tree_work.py:591–663`); all 57 stopping ceilings passed (`722–729`). M3 must preserve per-selection source admission, exact then folded reading, source/rule/state order and late hook identity while moving storage. It must not interpret fewer actual computations as fewer compatibility charges.

The proposed immutable account registration and subject binding (`M3:374–425`) addresses the reproduced drift and equal-OID authority hazards without freezing public counter fields. Context membership is not ancestry proof or permission to activate aggregate budgets (`231–240`). First-use authenticated reads and role/structural admission remain per account even when storage is shared (`341–360`). These distinctions are sufficient in shape; actual implementation remains unverified.

**Independent-owner boundary — CONFIRMED.** Separate PIDs, config baselines, future pooled charges, and independent abandonment are observable before ancestry is requested. D2/D5/D11/D13 and the executed command-sequence test establish that conflict. Keeping a narrow legacy pair adapter is the appropriate compatibility shape (`M3:242–284`). A coordinator could replace union-find mechanically, but it would still retain both owners and their admitted relationship. No cleaner solution preserving every stated signature and observable also achieves literal “one child, no linkage anywhere.” The record correctly refuses to call the adapter the new context and keeps full retirement behind a separate migration/contract gate.

**Environment correction — CONFIRMED.** Capture once prevents a verification from mixing ambient process inputs and directly serves issue #62 item 3. It changes behavior when HOME, PATH, locale or other preserved values change between phases. The maintainer should approve that named correction, with before/after traces, rather than suppressing it as refactor noise (`M3:158,282–291,317–327`). Preserve the five-variable preflight order and derive it and the owned children from the same captured input. Live ceilings and replaceable hooks must remain live. This is a reason to gate the correction explicitly, not to retain moving capture as the new context contract.

**5. Migration plan — CONFIRMED order and gates; CORRECTED nominal PR count.**

M3:493–499 has seven independently reviewable rows: PR1 characterization, PR2 owner/single-selection facade, PR3a accounts/raw objects, PR3b policy facts, PR4 composed verify, PR5 append/borrowed consumers, PR6 cleanup/full-retirement gate. The PR3a/PR3b split is necessary and present. Keep PR4/PR5 separate because their dispatch, initialization, and finalization barriers differ. No further mandatory split is established by this audit. The record already requires splitting D1 from PR2 if its approval cannot be independently reviewed there; PR6 must not treat approval to remove redundant forwarding as approval to delete the legacy adapter.

No missing requested gate was found. G includes all 3,189 current non-harness cases plus additions; 108 authenticated harness cases, both legs and zero skips; unchanged golden/refusal/wrapper/order bytes; real consumer suites at explicit revisions; tool/filesystem floors; full-suite execution before opening a PR; and maintainer plus independent review before merge (`M3:451–489`, `CONTRIBUTING.md:18–22,34–38`, supplied issue #62 Method). D1 and A7 approval precede ownership implementation (`493–495`). Whole-body SHA authentication, independent globals, reached-body counters, account attribution and complete failed-charge traces address the earlier false-comparison problem (`514–541`).

**6. Risks — CONFIRMED ranking and probe relevance; future execution UNVERIFIED.**

The folded table at M3:543–558 has twelve risks. No additional critical risk was established. Each proposed probe distinguishes its stated failure when its specified observations are asserted; existing tests alone do not discharge the new-context gates.

| Risk | Audit judgment |
| --- | --- |
| 1 critical: independent owners | Appropriate. PID/config/future-charge/abandonment observations decide the feasibility conflict; verdict equality alone cannot. |
| 2 critical: authority | Appropriate. Same-tree/commit/repository and replacement cases need both refusal-before-payload and valid-reuse controls; an always-refuse implementation must fail. |
| 3 critical: admission/precedence | Appropriate. Exact failed events, all fifteen counters, 57 stopping ceilings, activation barriers and zero premature I/O decide it. Final totals alone do not. |
| 4 high: fakes/subclasses | Appropriate. Exceptions plus zero authority construction and operative class/select dispatch are deciding observations. |
| 5 high: finalization | Appropriate. The full post-pass sentinel/config/child/cleanup/interrupt matrix is required. The current single close-config control does not settle it; the record now says so. |
| 6 high: framing/abandonment | Appropriate. Every same-context view must become terminal with no hidden restart; independent legacy owners must retain isolation. Both sides are necessary. |
| 7 critical pending A7 | Corrected appropriately from round 1. Packed/loose/duplicate, warmth, order, selection warming, replacement/tamper, authentication and account traces address Git state as well as Python caches. Contract choice is explicit; maintainer approval and implementation evidence remain open. |
| 8 high: capture/hooks | Appropriate. Recording every child across ambient mutations decides capture; separately patched limits/hooks distinguish freezing process input from freezing implementation seams. |
| 9 high: false equivalence | Appropriate. SHA authentication, reached old/new bodies, independent globals and a negative identical-leg control are necessary. Shared-child attribution needs an explicit account hook. |
| 10 medium: retained memory | Appropriate for memory growth alone. Measure actual retained bytes, membership, weak issuance and reclamation; public admission counters do not measure memory. |
| 11 high: consumers/hosts | Correctly elevated. Complete consumer bytes at revisions and host controls decide it; local imports or receipt harness success cannot substitute. |
| 12 high: physical evidence | Correctly separated from memory. lstat/spelling/open/fstat/read-once and short-write/cleanup controls decide physical safety; `DirectoryEvidence` or cache hits cannot. |

**7. Voice and precision — CONFIRMED.** The folded record removes the prior session/output narration and rhetorical boundary title. Historical PR3b/PR5 statements now cite exact correction commits and affected files (`M3:126,130–143`); own Git commands confirmed all four cited corrections belong to their cited merges. Maintainer review is correctly attributed to issue #62's Method (`459–461`), and D16 has a precise test range (`173`). All 148 explicit locator groups resolve; sampled source/test ranges support their claims. No remaining material unlocated factual claim was found. Proposals and future gates are explicitly labeled; recorded commands/results are evidence, not claims that the future context passed. Collection remains clearly distinct from execution (`614–639,727–731`). Historical M1 locators remain historical, as M3:8–12 explains.

Nothing independently confirmed in round 1 regressed in the exercised scope: source/tests and nineteen goldens are unchanged; the screen and precedence matrices, legacy pairing, moving environment trace, D12, subclass/callable seams and finalizer rendering remain reproducible. Full-suite and real-consumer execution are not inferred from that statement.

**Validation and limits.**

- Read the folded record, committed round-1 review, supplied issue #62, CONTRIBUTING.md and repository instructions. Ran source/test inspection with `rg`, numbered excerpts, and Git object reads.
- `PYTHONPATH=src .venv/bin/python -m pytest --collect-only -q`: **3,297 collected**.
- Compatibility execution: **593 passed, zero skips, 294.96 seconds**. Command used `.venv/bin/python -m pytest -q` with `PYTHONPATH=src`, `RECEIPT_LEDGER_TREE=/Users/maxghenis/TheAxiomFoundation/receipt/.extraction/ledger-9dafe81`, `RECEIPT_BRIER_TREE=/Users/maxghenis/TheAxiomFoundation/receipt/.extraction/brier-4b9e7be`, and the disagreement, precedence, golden, budgets, directory, inputs, consumer-import and four equivalence files. The harness split was **43/28/17/20**, authenticated locally, with no fetch/fallback and no expanded normalization.
- Focused execution: **102 passed, zero skips, 1,136 deselected, 47.68 seconds**. Included 67 protected-tree work/checkpoint/hash cases, four binding-seam cases, five subclass controls, two snapshot lifecycle cases, six snapshot-security cases, four shape controls, thirteen consumer-work controls, and one content-directory refusal. All 57 D12 stopping ceilings are included. JUnit files and `test-summary.json` record exact case identities and zero failures/errors/skips.
- Executed the record appendix, supplied packed driver unchanged, additional D2/D5/D8/D10/D11/D13 driver, independent 96-case transport matrix and distinct-commit/cross-repository identity construction. Both embedded Python examples compile.
- Verified 148 explicit locator groups, 19 byte-identical golden JSON files, equal production/tests against both post-M1 baselines, and the four cited historical correction/merge relationships. Review changes pass `git diff --check`.
- Host: **Python 3.14.4, Git 2.53.0, OpenSSL 3.6.3**. **UNVERIFIED:** complete 3,297-case execution; actual Chronicle/axiom-encode/thesis suites against this checkout; alternate minimum-version/filesystem hosts; new-context implementation and full A7/finalizer/account matrices; maintainer D1/A7 approvals. The **695 passing tests** are focused audit evidence.

---SUBFLEET-VERDICT-BEGIN---
{"schema_version": 1, "artifact_revision": {"kind": "file", "path": "docs/design/0.7-m3-repository-context.md", "commit": "9401e7bcad67e082bed967acb081392eb8bb2c48"}, "verdict": "approve", "findings": [], "notes": ["Performed one offline defensive correctness and completeness audit; no subagents, production/test edits, or design-record edits.", "Reexecuted the supplied packed-store probe and independently ran 96 loose/packed/duplicate replacement/removal/warmth/read-order controls; all matched the folded contract.", "Confirmed authenticated context-scoped availability is a sound proposed semantics, with explicitly lost cold-process refusal and possible verdict/work differences; separate maintainer A7 approval and implementation traces remain required.", "Reproduced M3 environment, config, six-field linkage, attribute isolation, reference drift, identity and abandonment probes; all M1 disagreement and precedence tests passed.", "Executed 593 compatibility tests including all 108 authenticated harness cases at zero skips, plus 102 focused controls including all 57 D12 stopping ceilings; 695 passed total.", "Collected 3297 cases, validated 148 explicit locator groups and 19 unchanged goldens, and inspected the requested consumer refs with git show/grep only.", "Full-suite, real-consumer, alternate-platform and new-context execution remain unverified; this design approval does not supply maintainer D1/A7 approval."]}
---SUBFLEET-VERDICT-END---

Delivery limitation: the filesystem sandbox rejected writes to `/Users/maxghenis/chief-of-staff/state/receipt-07/peer/m3-design-r2/review-full.md` and the adjacent `verdict.md` with `PermissionError: Operation not permitted`. The full report and identical output copy are committed at `audit/m3-design-r2/review-full.md` and `audit/m3-design-r2/verdict.md` in the review worktree. The final response supplies the same report for external `-o` capture. This is a filesystem permission restriction, not an automatic approval-review rejection.